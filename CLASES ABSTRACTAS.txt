CLASES ABSTRACTAS:
-es una clase que no se puede instanciar(crear objetos) pero si heredar
-puede tener constructores, genéricos pero no tiene operaciones
  - define el método pero no que hacen y las hijas son las que implementan(ponerle que es lo que hace) el método (sobreescribiendolo en cada clase)
- "abstract" en el diagrama todo lo que este en cursiva es abstracto
principios SOLID
MODIFICADORES
palabras reservadas que controlan quien puede acceder y desde donde
VISIBILIDAD: atributos
-public: cualquier parte
-private: solo la propia clase, se se quiere usar desde otra se usa un get o set.
-protected: se usa en subclases, aunque esten en otro paquete aun lo puede ver y lo que esta dentro del mismo paquete
-friendly(no tiene palabra reservada, ósea no se pone palabra y java lo cataloga asi): todos lo puede ver en el mismo paquete
PAQUETE: carpeta que agrupa clases iguales
-package
import: trae o indica el paquete al que se quiere referenciar

ARRAYLIST
importa ja.util.ArrayList
Integer=int : es una clase pq contiene  métodos para enteros
-Trabajo con tipo clase no primitivos (int,double)/se ponen la primera en mayuscula
declaracion:
ArrayList<String> nombres = new ArrayList<>();
ADD: si se agrega uno se corren los demas hacia la siguiente posicion
 frutas.add("Mango");
    frutas.add("Banano");
    frutas.add("Naranja");
    frutas.add(1, "fresa");
SET: con el set se reemplaza y se quita el q estaba en esa posicion 

EXCEPCIONES: son errores que pasan durante el tiempo de ejecución (cuando se interactúa con el código)
crash/el programa paro, ocurrió un error
try/catch:funciona tipo if else
-try: se pone el error que podría ocurrir, SI ese error sucede SIGUE al catch
-catch: captura el error en una variable e, y manda un mensaje de error dejando q el programa siga ejecutándose(no detiene el programa)
-finally: (se ejecuta siempre, haya error o no/opcional de ponerlo)

parseInt: método que convierte una variable a otro tipo string-int

